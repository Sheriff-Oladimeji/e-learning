import { marketing } from "../../../data/courses/courses";
import CourseDescription from "../../../components/courses/CourseDescription";

const MarketingDesc = () => {
  return <CourseDescription data={marketing} />;
};

export default MarketingDesc;
